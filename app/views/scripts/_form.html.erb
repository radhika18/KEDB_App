<%= form_for(@script) do |f| %>
  <% if @script.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@script.errors.count, "error") %> prohibited this script from being saved:</h2>

      <ul>
      <% @script.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form">
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title , :placeholder => "Title of the script" , :required => 'required' %>
  </div>
  <div class="field">
    <%= f.label :procedure %><br>
    <%= f.text_area :procedure , :placeholder => "Type the script" , :required => 'required' %>
  </div>
  <div class="actions">
   <div class="bas">
    <%= f.submit :class => 'button' , :value => 'Submit Script' %>
    </div>
  </div>
<% end %>
</div>
